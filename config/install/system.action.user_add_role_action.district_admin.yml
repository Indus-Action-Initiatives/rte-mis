langcode: en
status: true
dependencies:
  config:
    - user.role.district_admin
  module:
    - user
id: user_add_role_action.district_admin
label: 'Add the District Admin role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: district_admin
