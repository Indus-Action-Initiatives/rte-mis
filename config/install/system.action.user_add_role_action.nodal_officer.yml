langcode: en
status: true
dependencies:
  config:
    - user.role.nodal_officer
  module:
    - user
id: user_add_role_action.nodal_officer
label: 'Add the Nodal Officer role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: nodal_officer
