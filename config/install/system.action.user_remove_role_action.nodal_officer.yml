langcode: en
status: true
dependencies:
  config:
    - user.role.nodal_officer
  module:
    - user
id: user_remove_role_action.nodal_officer
label: 'Remove the Nodal Officer role from the selected user(s)'
type: user
plugin: user_remove_role_action
configuration:
  rid: nodal_officer
